const aluno1 = {
    nome: 'Maria da Silva',
    idade: 19,
    nota: 8.6
}

const aluno2 = {
    nome: 'João Martins',
    idade: 22,
    nota: 7
}

// Média (nota aluno 1 + anota aluno 2) / 2

const media = (aluno1.nota + aluno2.nota) / 2
console.log(media)